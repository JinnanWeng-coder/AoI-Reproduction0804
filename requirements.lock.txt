# Top-level packages validated in aoi_v2x on 2026-08-04 (CPU).
# Install the CPU PyTorch wheel separately as documented in
# ENVIRONMENT_INSTALL.md; this file intentionally does not select a torch
# runtime.
numpy==1.23.5
scipy==1.13.1
matplotlib==3.9.4
PyYAML==6.0.3
pytest==8.4.2
